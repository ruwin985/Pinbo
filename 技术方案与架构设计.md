# 拍呗（Pinbo）— 技术方案与架构设计

> App 名称：**拍呗** ｜ 项目工程名：**Pinbo**
> 技术栈：**iOS：UIKit + AVFoundation** ｜ **macOS：AppKit + ScreenCaptureKit + AVFoundation**
> 目标平台：
> - **iOS 13+**（多摄需 A12 芯片及以上，即 iPhone XS/XR 起；推荐 iPhone 12/A14 起）
> - **macOS 12.3+**（ScreenCaptureKit 要求）
> 版本：v0.2 方案稿 · 2026-08-15（新增跨平台架构与三方依赖章节）

---

## 1. 产品概述

一款面向短视频博主的高效创作工具，核心能力：

1. **前后双摄同框录制**：后摄拍场景，前摄拍人物（画中画）。
2. **实时字幕**：录制时把说的话实时转成文字，叠加在画面上（中英文）。
3. **可拖拽画中画**：录制/编辑时，前摄小窗的位置、大小、圆角可随时调整。
4. **一体化编辑页**：录完进入编辑页，可修改视频、字幕文字、背景音乐。
5. **一键导出**：竖版视频，适配抖音 / 微信视频号等平台。

**核心设计原则：录制轻量、编辑非破坏、导出统一合成。**

---

## 2. 核心架构决策

### 2.1 双摄录制策略（最关键的决策）

| 方案 | 描述 | 结论 |
|------|------|------|
| A. 录制时实时合成一路视频 | 边录边把前后摄合成，画中画位置录死 | ❌ 无法后期调整画中画，性能负担大 |
| **B. 分路录制 + 元数据驱动** | **前后摄各存一路原始视频，画中画布局/字幕作为"时间轴元数据"单独保存，导出时才合成** | ✅ **采用** |

**方案 B 的优势：**
- 录制时只做预览渲染，不做实时合成 → 更流畅、更省电。
- 画中画位置/大小/圆角、字幕文字，录后仍可在编辑页自由修改（非破坏性）。
- 导出时用 `AVMutableComposition` + `AVVideoComposition` 一次性精确合成。

**代价：** 需要自己管理"两路视频 + 元数据 → 合成"的时间轴对齐逻辑（下文 §5 详述）。

### 2.2 数据模型（非破坏性编辑的核心）

一个"项目（Project）"包含：

```
Project
├── backVideoURL          // 后摄原始视频文件
├── frontVideoURL         // 前摄原始视频文件
├── audioSource           // 原声（来自哪一路）/ 背景音乐
├── duration              // 总时长
├── pipTrack: [PiPKeyframe]   // 画中画布局关键帧（随时间变化）
├── subtitleTrack: [SubtitleSegment]  // 字幕分段
└── musicTrack?           // 背景音乐 + 音量/淡入淡出
```

```
PiPKeyframe
├── time                  // 时间点（秒）
├── center (x, y)         // 归一化坐标 0~1
├── size (w, h)           // 归一化尺寸
└── cornerRadius          // 圆角

SubtitleSegment
├── startTime / endTime   // 起止时间戳（来自语音识别）
├── text                  // 可编辑文字
└── style                 // 字体/颜色/描边/位置/动画
```

> **关键点**：字幕必须保存**精确时间戳**，画中画布局用**关键帧**记录，这样编辑页才能精准回放与修改。

### 2.3 设备兼容性（调研结论）

**iOS（前后双摄同录）：**
- API `AVCaptureMultiCamSession` 要求 **iOS 13.0+**（Apple 官方文档确认）。
- 硬件门槛为 **A12 芯片及以上** → 最低机型 **iPhone XS / XS Max / XR（2018）**。
- 运行时必须用 `AVCaptureMultiCamSession.isMultiCamSupported` 检测，不能仅靠系统版本。
- **性能分级建议**（需求为双摄 + 实时字幕 + 预览，高负载）：
  - 技术支持下限：iPhone XS（A12），需降分辨率（后摄 1080p / 前摄 720p）+ 发热提示。
  - **实际推荐下限：iPhone 12（A14）起**，流畅且发热可控。
  - iPhone 14 及以后无压力。

**Android（供参考，首期不做）：**
- Android 11（API 30）起提供 `getConcurrentCameraIds()`，但为**可选能力**，厂商可不实现。
- **中低端机基本不支持**前后同开（ISP 带宽/内存不足）；仅部分旗舰支持且能同开的分辨率常被限制。
- 碎片化严重、逐机型适配成本高 → **首期只做 iOS 是最优解**。

**macOS：** `ScreenCaptureKit` 要求 macOS 12.3+；摄像头单路采集无特殊硬件门槛。

---

## 3. 跨平台架构（iOS + macOS 一个项目）

### 3.0 平台策略

**结论：一个 Xcode 项目 + 共享 Core 框架 + 各平台独立采集层与 UI。不做两个独立 App。**

两个平台业务本质相同，差异只在"输入源"：

| | iPhone（iOS） | Mac（macOS） |
|---|--------------|--------------|
| 大窗口（主画面） | 后摄像头 | **录屏**（窗口 / App / 桌面 / 全屏） |
| 小窗口（画中画） | 前摄像头 | Mac 摄像头 |
| 字幕 / 编辑 / 合成 / 导出 | **完全一致，共用 Core** | **完全一致，共用 Core** |

约 60-70% 代码可共享（数据模型、字幕、编辑、合成导出）。**关键抽象：把"输入源"封装成协议 `CaptureSourceProviding`，产出统一的"两路视频流 + 音频"，上层无感知平台差异。**

### 3.1 目录结构（跨平台）

```
Pinbo/
├── Core/  (共享 Swift Package / Framework，iOS + macOS 通用)
│   ├── Models/            // Project / PiPKeyframe / SubtitleSegment
│   ├── Speech/            // LiveSpeechRecognizer (SFSpeechRecognizer 中/英)
│   ├── Editor/            // 编辑逻辑（非破坏性，纯数据操作）
│   ├── Composition/       // 合成导出（AVMutableComposition / 导出）
│   │   ├── VideoCompositor
│   │   ├── PiPLayerInstruction   // 画中画层指令（Core Animation）
│   │   ├── SubtitleRenderer      // 字幕渲染进视频（CATextLayer）
│   │   └── ExportManager         // AVAssetExportSession
│   └── Storage/           // 项目持久化
│
├── CaptureSource/  (采集抽象层)
│   ├── CaptureSourceProviding.swift   // 协议：输出两路视频 + 音频
│   ├── iOS/DualCameraSource.swift     // AVCaptureMultiCamSession 实现
│   └── macOS/ScreenCameraSource.swift // ScreenCaptureKit + 摄像头 实现
│
├── iOS-App/   (UIKit)
│   ├── RecordingViewController   // 双摄预览 + 手势拖拽画中画 + 实时字幕
│   ├── EditorViewController
│   └── ...（触摸交互 UI）
│
└── macOS-App/ (AppKit)
    ├── RecordingWindowController // 录屏源选择 + 摄像头小窗 + 实时字幕
    ├── EditorWindowController
    └── ...（鼠标 / 窗口交互 UI）
```

> **UI 不强行共享**：iOS 触摸手势、macOS 鼠标/窗口交互差异大，各写原生 UI（UIKit / AppKit），但都调用同一套 Core。不建议用 Mac Catalyst，因为 Mac 有录屏这种原生需求，AppKit 体验更好。

### 3.2 macOS 端关键技术

- **录屏**：`ScreenCaptureKit`（macOS 12.3+），支持选**单个窗口 / 单个 App / 整个桌面 / 全屏**，正好满足需求；性能优于旧的 `CGDisplayStream`。
- **摄像头小窗**：`AVCaptureSession` 单路即可（Mac 无需 MultiCam，无发热/性能压力）。
- **合成 / 字幕 / 导出**：直接复用 Core 中的 AVFoundation 代码。
- **好处**：Mac 端比 iPhone 端更简单，不涉及 MultiCam 的性能瓶颈。

### 3.3 上架与商业化

- 推荐 **Universal Purchase / 跨平台通用订阅**：博主常"手机拍 vlog + 电脑录教程"，用户高度重叠，买一次两端可用，体验最好、更利于卖订阅。
- 订阅用 `StoreKit 2`，跨平台共享订阅状态。

### 3.4 落地节奏（不影响先做 iPhone）

1. 先做 iPhone MVP 并验证需求；**从一开始就把可复用逻辑放进 Core 框架**，为 Mac 铺路。
2. iPhone 验证跑通后，新增 macOS Target，只需实现 `ScreenCameraSource` 采集源即可复用大部分代码。

---

## 4. 关键技术点详解

### 4.1 前后双摄同时录制

- 使用 `AVCaptureMultiCamSession`（iOS 13+）。
- 用 `AVCaptureMultiCamSession.isMultiCamSupported` 检测设备支持（A12+）。
- 前后摄各配一个 `AVCaptureDeviceInput` + `AVCaptureVideoDataOutput`（或 `AVCaptureMovieFileOutput` 分路直录）。
- **推荐**：用两个 `AVAssetWriter` 分别把前/后摄写成两个文件，音频挂在其中一路（通常后摄/主麦克风）。

⚠️ **注意事项：**
- MultiCam 对性能和发热要求高，需限制分辨率（如两路都 1080p 可能过热，可后摄 1080p、前摄 720p）。
- 处理中断（来电、后台）需保存已录片段。
- 首次需申请相机 + 麦克风权限。

### 4.2 画中画预览（录制时可拖拽/缩放/圆角）

- 录制阶段：`PiPPreviewView` 是一个承载前摄预览层的 UIView，加 `UIPanGestureRecognizer`（拖动）、`UIPinchGestureRecognizer`（缩放）。圆角用 `layer.cornerRadius`。
- 用户每次调整 → 生成一个 `PiPKeyframe` 记入 `pipTrack`（带当前时间戳）。
- **注意**：预览层的坐标是屏幕坐标，存储时要**归一化**（除以画布尺寸），保证不同尺寸导出时布局一致。

### 4.3 实时字幕（中英文）

- `SFSpeechRecognizer`，`locale` 支持中英切换（`zh-CN` / `en-US`）。
- 用 `SFSpeechAudioBufferRecognitionRequest` 做**流式**识别，`shouldReportPartialResults = true` 实时显示。
- 每段结果的 `segments` 带 `timestamp` 和 `duration` → 存进 `SubtitleTrack`。

⚠️ **限制与对策：**
- 系统语音识别有**单次时长限制（约 1 分钟）和频率限制**，长视频需**分段重启识别请求**并拼接时间戳。
- 中英混说识别效果有限，MVP 先让用户**选主语言**，V2 再考虑双语引擎或云端 ASR（如需更高准确率可接入第三方，但增加成本）。
- 需处理断句、标点缺失（可后期在编辑页手动修正）。

### 4.4 编辑页（非破坏性）

- 编辑页读取 Project 数据，用**预览合成**（`AVPlayer` + `AVVideoComposition` 实时预览）而非重新导出。
- 修改字幕文字 / 画中画布局 / 音乐 → 只改数据模型，实时刷新预览。
- 这样编辑流畅、可反复调整，直到用户点"导出"才真正渲染。

### 4.5 合成导出

- `AVMutableComposition`：把后摄作为底层轨道、前摄作为画中画轨道。
- `AVMutableVideoCompositionLayerInstruction`：按 `pipTrack` 关键帧设置前摄的 `transform`（位置/缩放），圆角用 `CALayer` mask。
- 字幕：用 Core Animation 图层（`CATextLayer`）挂到 `AVVideoComposition.animationTool`，按时间戳显隐。
- 背景音乐：用 `AVMutableAudioMix` 混音（原声 + 音乐音量控制）。
- 最终用 `AVAssetExportSession` 导出竖版 1080x1920 mp4。

---

## 5. 时间轴对齐（易踩坑）

由于前后摄是分路录制的，导出时必须保证两路**时间对齐**：

- 录制时记录两路的起始时间戳（`CMTime`），以较晚开始的一路为基准裁齐。
- 字幕时间戳、画中画关键帧都以"项目时间轴（从 0 开始）"为准，而非各自设备时间。
- 建议录制时用**同一个 `CMClock`** 驱动两路 writer，减少漂移。

---

## 5.5 三方依赖与成本分析（是否需要大模型 / 付费技术）

**核心结论：MVP 阶段几乎零外部技术成本，全部核心功能都用 Apple 系统框架即可实现，无需任何大模型或第三方付费服务。**

### 各功能所需技术盘点

| 功能 | 用什么实现 | 是否需大模型/三方付费 | 成本 |
|------|-----------|---------------------|------|
| 前后双摄同录 | `AVCaptureMultiCamSession`（系统） | 否 | 免费 |
| Mac 录屏 | `ScreenCaptureKit`（系统） | 否 | 免费 |
| 实时字幕（语音转文字） | `SFSpeechRecognizer`（系统，设备端） | **否** | 免费 |
| 画中画拖拽 | UIKit/AppKit 手势（系统） | 否 | 免费 |
| 视频合成/字幕渲染/导出 | `AVFoundation` + Core Animation（系统） | 否 | 免费 |
| 背景音乐 | 自备曲库 / 用户导入 | 否 | 免费* |
| 订阅内购 | `StoreKit 2`（系统） | 否 | 苹果抽成 |

> \* 背景音乐若用**商用曲库**需买版权授权（见下）。

### 唯一"可能"要花钱的地方

1. **Apple 开发者账号**：$99/年（上架必需，硬性成本）。
2. **背景音乐版权**：如果内置曲库供用户使用，需购买**商用授权音乐**（如 Epidemic Sound、Artlist 等，年费订阅），或只提供"用户自行导入音乐"来规避。**MVP 建议先只让用户导入，避免版权成本。**
3. **（可选，非必需）云端语音识别**：`SFSpeechRecognizer` 免费但有以下局限：
   - 单次识别有**时长限制（约 1 分钟）**，长视频需分段拼接。
   - **中英混说**识别精度一般。
   - 若未来要更高精度/更好的中英混合，**可选**接入第三方云端 ASR（如讯飞、阿里云、Whisper API 等），**这才会产生按量付费成本**。但这是 **V2 的优化项，MVP 完全不需要**。

### 结论对你的意义

- **启动成本极低**：除了 $99/年开发者账号，MVP 阶段没有任何强制的三方付费或大模型开销。
- **无按量成本压力**：不像很多 AI 应用要为每次调用付费（如 GPT/图像生成 API），你的核心功能都在**设备端本地完成**，用户越多也不增加你的服务器/API 成本 —— 这对副业和利润率非常友好。
- **未来若要提升字幕精度**，再评估是否引入云端 ASR，届时可把成本转嫁到订阅高级功能里。

---

## 6. 商业模式

- **免费 + 订阅（Freemium）**：
  - 免费版：带水印 / 限时长（如 30s）/ 基础字幕样式。
  - 订阅版：无水印、高清导出、全部布局模板与字幕样式、背景音乐库。
- 定价参考：¥25/月 或 ¥168/年（对标同类工具）。
- 用 `StoreKit 2` 做内购订阅。

---

## 7. 开发路线图（副业节奏，先验证再投入）

### 阶段 0：技术验证（最高优先级）
- [ ] 验证目标机型上 `AVCaptureMultiCamSession` 双摄同录可行、不过热。
- [ ] 验证 `SFSpeechRecognizer` 实时字幕 + 长视频分段拼接。
- [ ] 验证分路录制 + 元数据驱动导出的合成效果。

> 这三点跑通，产品即成立。

### 阶段 1：MVP
- [ ] 录制页：双摄预览 + 可拖拽画中画 + 实时字幕。
- [ ] 分路录制存文件 + 元数据。
- [ ] 编辑页：改字幕文字、改画中画布局。
- [ ] 导出竖版视频。
- [ ] 上架 TestFlight，找 10-20 个博主试用。

### 阶段 2：完善（验证有需求后）
- [ ] 背景音乐、字幕样式、多布局模板。
- [ ] 订阅内购。
- [ ] 平台尺寸预设（抖音/视频号）。

### 阶段 3：增长
- [ ] 数据埋点（下载→付费转化、留存）。
- [ ] 根据数据决定是否加大投入。

---

## 8. 主要风险与对策

| 风险 | 对策 |
|------|------|
| 双摄性能/发热 | 限制分辨率、限机型、做降级方案 |
| 系统语音识别精度/时长限制 | 分段拼接；V2 评估云端 ASR |
| 中英混说识别差 | MVP 先选单一主语言 |
| 竞品（剪映等）功能覆盖 | 聚焦"双摄同框+实时字幕+快产出"的组合体验与效率 |
| 时间轴漂移导致音画不同步 | 统一时钟驱动、起始对齐 |

---

## 9. 下一步

技术验证阶段的三个点是成败关键。建议下一步**先写一个最小 Demo 验证"双摄同录 + 实时字幕 + 可拖拽画中画预览"**，其余功能都建立在此之上。

需要时我可以基于本方案直接开始搭建 UIKit 工程骨架和核心录制模块代码。
